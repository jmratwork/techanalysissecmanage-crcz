# Filebeat configuration
filebeat.inputs:
  - type: log
    paths:
      - /var/log/*.log
output.logstash:
  hosts: ["{{ siem_ingest_host | default(siem_host | default('localhost')) }}:{{ siem_ingest_port | default(5044) }}"]
  protocol: "{{ siem_ingest_protocol | default('tcp') }}"
